{
  "name": "remark-lint-word-case",
  "type": "module",
  "version": "1.0.0",
  "description": "remar-lint rule that checks Markdown documents to ensure specified words appear with the correct casing.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "bun build src/*.ts --outdir dist --target node --sourcemap && tsc --emitDeclarationOnly",
    "test": "bnt"
  },
  "keywords": [
    "remark",
    "remark-lint",
    "remark-lint-rule",
    "remark-plugin",
    "markdown",
    "lint",
    "word-case",
    "word-casing",
    "capitalization"
  ],
  "author": "Marco Bulgarelli",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "dependencies": {
    "remark-cli": "^12.0.1",
    "remark-lint": "^10.0.1",
    "unified-lint-rule": "^3.0.1",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.20",
    "@types/unist": "^3.0.3",
    "@types/vfile": "^4.0.0",
    "prettier": "^3.6.2",
    "remark": "^15.0.1",
    "vfile": "^6.0.3"
  },
  "prettier": {
    "singleQuote": false,
    "trailingComma": "all",
    "printWidth": 80,
    "tabWidth": 2,
    "semi": true
  },
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  }
}
